# Root .env.example — see also apps/nextjs/.env.example and apps/expo/.env.example
# for app-specific variables.

# No database — clt-app uses JSON files in data/ as its data store.
# No auth — community submissions use anonymous device IDs + GitHub App bot.

# See docs/operations/secrets.md for the full secret reference.
